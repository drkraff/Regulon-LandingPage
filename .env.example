# Turso (libSQL): create a DB at https://turso.tech or run:
#   npx turso db create regulon-waitlist
# Then: npx turso db show regulon-waitlist --url  and  npx turso db tokens create regulon-waitlist
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Admin: HTTP Basic Auth for /api/admin/* (browser prompt)
ADMIN_USERNAME=
ADMIN_PASSWORD=
